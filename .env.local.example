# ─────────────────────────────────────────────────────────────
# VISTARA — Environment Variables
# Copy this file to .env.local and fill in the values below.
# ─────────────────────────────────────────────────────────────

# ── REQUIRED: Supabase ───────────────────────────────────────
# Get these from: https://app.supabase.com/project/wxqbapavtmqbrldvoxla/settings/api

# Public URL (safe to expose in browser)
NEXT_PUBLIC_SUPABASE_URL=https://wxqbapavtmqbrldvoxla.supabase.co

# Anon / public key (used by browser client, respects RLS)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Service role key (server-side only — NEVER expose to browser)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ── REQUIRED: NextAuth ────────────────────────────────────────
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-random-secret-here
NEXTAUTH_URL=http://localhost:3000

# ── OPTIONAL: Run Migrations ─────────────────────────────────
# Add your database password to use `npm run migrate` instead of
# manually pasting SQL in the Supabase SQL Editor.
# Get it from: https://app.supabase.com/project/wxqbapavtmqbrldvoxla/settings/database
# SUPABASE_DB_PASSWORD=your-database-password

# ── OPTIONAL: OpenAI (activates GPT-4o chat) ─────────────────
# Without this key, Vistara uses its context-aware template engine.
# Add your key and the chat will automatically upgrade to real AI.
# OPENAI_API_KEY=sk-...

# ── OPTIONAL: Mapbox (activates interactive maps) ────────────
# Without this token, Vistara uses the SVG route map with real coordinates.
# Add your token and the map will automatically upgrade to Mapbox GL.
# NEXT_PUBLIC_MAPBOX_TOKEN=pk...
